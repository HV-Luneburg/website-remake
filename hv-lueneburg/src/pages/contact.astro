---
import Layout from '~/layouts/PageLayout.astro';
import { Icon } from 'astro-icon/components';

const metadata = {
  title: 'Kontakt | HV Lüneburg',
};
---

<Layout metadata={metadata}>

  <!-- Hero (minimal, black) -->
  <section class="relative bg-black text-white pt-28 pb-16 border-b-4 border-primary overflow-hidden">
    <span class="absolute right-0 bottom-0 font-heading font-black text-white/[0.03] pointer-events-none select-none leading-none"
          style="font-size: clamp(6rem, 18vw, 14rem);">HALLO</span>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 relative z-10">
      <span class="heading-eyebrow">Kontakt</span>
      <h1 class="mt-2 font-heading font-black uppercase tracking-tight text-white leading-tight"
          style="font-size: clamp(2.5rem, 7vw, 5rem);">
        Schreib uns!
      </h1>
      <p class="mt-4 text-gray-400 text-lg max-w-2xl leading-relaxed">
        Fragen zum Verein, zum Training oder möchtest du bei uns mitspielen? Wir antworten in der Regel innerhalb weniger Tage.
      </p>
    </div>
  </section>

  <!-- Contact content -->
  <section class="bg-white dark:bg-[#141414] py-16 md:py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="grid grid-cols-1 lg:grid-cols-5 gap-12 lg:gap-16">

        <!-- Left: contact info -->
        <div class="lg:col-span-2 space-y-8">
          <div>
            <span class="heading-eyebrow">So erreichst du uns</span>
            <h2 class="mt-2 text-2xl font-heading font-black uppercase text-gray-900 dark:text-white">Direkt in Kontakt treten</h2>
          </div>

          <div class="space-y-5">
            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-xl bg-primary/10 flex items-center justify-center flex-shrink-0">
                <Icon name="tabler:mail" class="w-5 h-5 text-primary" />
              </div>
              <div>
                <p class="text-xs font-black uppercase tracking-widest text-gray-400 dark:text-gray-500 mb-0.5">E-Mail</p>
                <a href="mailto:info@hv-lueneburg.de" class="text-gray-900 dark:text-white font-semibold hover:text-primary transition-colors">
                  info@hv-lueneburg.de
                </a>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-xl bg-primary/10 flex items-center justify-center flex-shrink-0">
                <Icon name="tabler:map-pin" class="w-5 h-5 text-primary" />
              </div>
              <div>
                <p class="text-xs font-black uppercase tracking-widest text-gray-400 dark:text-gray-500 mb-0.5">Adresse</p>
                <p class="text-gray-900 dark:text-white font-semibold">Platzhalter-Straße 1<br/>21335 Lüneburg</p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-10 h-10 rounded-xl bg-primary/10 flex items-center justify-center flex-shrink-0">
                <Icon name="tabler:brand-instagram" class="w-5 h-5 text-primary" />
              </div>
              <div>
                <p class="text-xs font-black uppercase tracking-widest text-gray-400 dark:text-gray-500 mb-0.5">Instagram</p>
                <a href="#" class="text-gray-900 dark:text-white font-semibold hover:text-primary transition-colors">
                  @hvlueneburg
                </a>
              </div>
            </div>
          </div>          
        </div>

        <!-- Right: contact form -->
        <div class="lg:col-span-3">
          <form
            name="contact"
            method="POST"
            class="bg-white dark:bg-[#1C1C1C] rounded-2xl border border-gray-100 dark:border-[#2A2A2A] shadow-sm p-8 space-y-6"
          >
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <div>
                <label for="name" class="block text-xs font-black uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-2">Name</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  required
                  placeholder="Max Mustermann"
                  class="w-full bg-gray-50 dark:bg-[#242424] border border-gray-200 dark:border-[#3A3A3A] rounded-xl px-4 py-3 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:ring-2 focus:ring-primary/40 focus:border-primary transition-colors text-sm"
                />
              </div>
              <div>
                <label for="email" class="block text-xs font-black uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-2">E-Mail</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  required
                  placeholder="max@beispiel.de"
                  class="w-full bg-gray-50 dark:bg-[#242424] border border-gray-200 dark:border-[#3A3A3A] rounded-xl px-4 py-3 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:ring-2 focus:ring-primary/40 focus:border-primary transition-colors text-sm"
                />
              </div>
            </div>

            <div>
              <label for="message" class="block text-xs font-black uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-2">Nachricht</label>
              <textarea
                id="message"
                name="message"
                rows="6"
                required
                placeholder="Hallo, ich interessiere mich für..."
                class="w-full bg-gray-50 dark:bg-[#242424] border border-gray-200 dark:border-[#3A3A3A] rounded-xl px-4 py-3 text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:ring-2 focus:ring-primary/40 focus:border-primary transition-colors text-sm resize-none"
              ></textarea>
            </div>

            <div class="flex items-start gap-3">
              <input
                type="checkbox"
                id="disclaimer"
                name="disclaimer"
                required
                class="mt-1 w-4 h-4 accent-primary flex-shrink-0"
              />
              <label for="disclaimer" class="text-xs text-gray-500 dark:text-gray-400 leading-relaxed">
                Mit dem Absenden dieses Formulars erklärst du dich mit der Verarbeitung deiner Daten einverstanden.
              </label>
            </div>

            <button
              type="submit"
              class="btn btn--primary btn--pill w-full sm:w-auto"
            >
              Nachricht senden
              <Icon name="tabler:send" class="w-4 h-4" />
            </button>

            <p class="text-xs text-gray-400 dark:text-gray-500">
              Wir antworten in der Regel innerhalb weniger Tage.
            </p>
          </form>
        </div>

      </div>
    </div>
  </section>

</Layout>
